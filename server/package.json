{
  "name": "sra-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "build": "npm --prefix ../client install && npm --prefix ../client run build",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "serverless-http": "^3.0.0"
  }
}
